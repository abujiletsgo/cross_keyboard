if SHIELD_CROSSES_TP_RIGHT

config ZMK_KEYBOARD_NAME
	default "Crosses TP"

config ZMK_SPLIT_BLE_ROLE_CENTRAL
	default y

endif

if SHIELD_CROSSES_TP_LEFT || SHIELD_CROSSES_TP_RIGHT

config ZMK_SPLIT
	default y

endif
